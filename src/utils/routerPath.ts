export enum RouterPathAdmin {
  CURSOS = "/cursos",
  CURSOS_ID = "/cursos/:id",

  CATEGORIAS = "/categorias",
  CATEGORIAS_ID = "/categorias/:id",
}

export enum RouterPathAuth {
  INICIAR_SESION = "/iniciar-sesion",
  REGISTRAR_USUARIO = "/registrar-usuario",
}

export enum RouterPathComment {
  CURSO_ID = "/:cursoId",
  ID = "/:id",
}
